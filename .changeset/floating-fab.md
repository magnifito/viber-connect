---
"@puralex/viber-connect-web-component": minor
"@puralex/viber-connect-react": minor
---

Improve the floating (`fab`) variant: it now sits as a round bubble bottom-right, pulses gently to
draw attention (respecting `prefers-reduced-motion`), and expands to a "Chat on Viber" pill on
hover/focus. The label is kept in the DOM for screen readers.
